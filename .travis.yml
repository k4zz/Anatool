os: linux
language: python
python:
  - 3.9
install:
  - pip install -r requirements.txt
  - pip install pyinstaller
deploy:
  provider: releases
  api_key: $RELEASE_TOKEN
  file: "dist/*.exe"
  cleanup: false
  draft: true
